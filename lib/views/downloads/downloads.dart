import 'package:flutter/material.dart';

class Downloads extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
     return Scaffold(
      appBar: AppBar(title: Text("Downloads"),),
      body: Center(
        child: Text("This is supposed to be the dwnldpg page"),
      ),
    );
  }
}
